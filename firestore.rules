rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access — anyone with the URL can read and write.
    // Tighten this later if you add authentication.
    match /assets/{assetId} {
      allow read, write: if true;
    }
  }
}
